---
phase: 02-xapi-event-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - xapi-profile/academic-literacies-profile.jsonld
  - xapi-profile/docs/vocabulary-mapping.md
  - xapi-profile/docs/event-catalog.md
autonomous: true

must_haves:
  truths:
    - "A JSON-LD xAPI Profile document defines all domain-specific activity types, extensions, and reused verbs for academic literacy tracking"
    - "Zero custom verbs are defined -- all events use existing standard verbs (ADL, Jisc, TinCan, Activity Streams)"
    - "9 custom activity types are defined with IRIs under https://w3id.org/xapi/aclit/v1/activity-types/"
    - "~15 custom extensions are defined with inlineSchema JSON Schema validation on each"
    - "Every event type in the catalog references the DPIA and documents its pedagogical purpose"
    - "A vocabulary mapping traces every verb and activity type to its source vocabulary"
  artifacts:
    - path: "xapi-profile/academic-literacies-profile.jsonld"
      provides: "Complete xAPI Profile with concepts array containing all activity types and extensions"
      contains: "conformsTo"
    - path: "xapi-profile/docs/vocabulary-mapping.md"
      provides: "Mapping of every verb and activity type to source vocabulary with IRI"
      contains: "ADL"
    - path: "xapi-profile/docs/event-catalog.md"
      provides: "Human-readable event catalog with DPIA references and pedagogical purpose"
      contains: "Pedagogical Purpose"
  key_links:
    - from: "xapi-profile/academic-literacies-profile.jsonld"
      to: "xapi-profile/docs/vocabulary-mapping.md"
      via: "Every concept IRI in the profile has a corresponding entry in the vocabulary mapping"
    - from: "xapi-profile/docs/event-catalog.md"
      to: "governance/dpia/01-processing-description.md"
      via: "Each event type references a DPIA data collection activity by section number"
---

<objective>
Create the xAPI Profile document (JSON-LD), vocabulary mapping, and event catalog for the Academic Literacies learning analytics system.

Purpose: The profile document is the machine-readable specification that defines all custom vocabulary (activity types, extensions) for the system. The vocabulary mapping documents the provenance of every term. The event catalog is the human-readable reference that links each event type to its pedagogical purpose and DPIA section, satisfying Success Criterion 4 ("every event type references the DPIA and documents its pedagogical purpose").

Output: Three files -- a JSON-LD profile document, a vocabulary mapping markdown, and an event catalog markdown with DPIA cross-references.
</objective>

<execution_context>
@C:\Users\mwmus\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\mwmus\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\mwmus\ALE00Y1\academic-literacies\.planning\PROJECT.md
@C:\Users\mwmus\ALE00Y1\academic-literacies\.planning\ROADMAP.md
@C:\Users\mwmus\ALE00Y1\academic-literacies\.planning\STATE.md
@C:\Users\mwmus\ALE00Y1\academic-literacies\.planning\phases\02-xapi-event-schema\02-CONTEXT.md
@C:\Users\mwmus\ALE00Y1\academic-literacies\.planning\phases\02-xapi-event-schema\02-RESEARCH.md
@C:\Users\mwmus\ALE00Y1\academic-literacies\governance\dpia\01-processing-description.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create xAPI Profile Document (JSON-LD) with All Concepts</name>
  <files>xapi-profile/academic-literacies-profile.jsonld</files>
  <action>
Create the xapi-profile/ directory structure and the main profile document.

**Profile metadata:**
- `@context`: `"https://w3id.org/xapi/profiles/context"`
- `id`: `"https://w3id.org/xapi/aclit"`
- `type`: `"Profile"`
- `conformsTo`: `"https://w3id.org/xapi/profiles#1.0"`
- `prefLabel`: `{"en": "Academic Literacies Learning Analytics Profile"}`
- `definition`: `{"en": "xAPI Profile for tracking student interactions in an academic literacies course. Defines activity types, extensions, statement templates, and patterns for reading, writing, quiz, discussion, navigation, help-seeking, and peer review events."}`
- `author`: `{"type": "Organization", "name": "Academic Literacies Teaching Team"}`
- `versions`: one entry with `id`: `"https://w3id.org/xapi/aclit/v1"` and `generatedAtTime`: `"2026-03-07T00:00:00Z"`

**Concepts array -- Activity Types (9 custom):**

For each, include: `id`, `type` ("ActivityType"), `inScheme` ("https://w3id.org/xapi/aclit/v1"), `prefLabel`, `definition`, and `broadMatch` or `relatedMatch` where a standard type is close.

1. `https://w3id.org/xapi/aclit/v1/activity-types/session` -- "Learning Session" -- container for a single login-to-logout user session. broadMatch: none (unique to session-scoped context pattern).
2. `https://w3id.org/xapi/aclit/v1/activity-types/reading-section` -- "Reading Section" -- a page or section of reading content within a module. broadMatch: `http://adlnet.gov/expapi/activities/lesson`.
3. `https://w3id.org/xapi/aclit/v1/activity-types/writing-task` -- "Writing Task" -- a writing assignment requiring student-authored text (paragraph, essay, reflection, report). broadMatch: `http://activitystrea.ms/schema/1.0/article`.
4. `https://w3id.org/xapi/aclit/v1/activity-types/discussion-thread` -- "Discussion Thread" -- a forum discussion thread containing posts and replies. broadMatch: `http://id.tincanapi.com/activitytype/forum-topic`.
5. `https://w3id.org/xapi/aclit/v1/activity-types/peer-review` -- "Peer Review" -- a peer review activity where one student reviews another's writing. broadMatch: none.
6. `https://w3id.org/xapi/aclit/v1/activity-types/help-request` -- "Help Request" -- a request for academic help or support from a tutor or support service. broadMatch: none.
7. `https://w3id.org/xapi/aclit/v1/activity-types/help-resource` -- "Help Resource" -- a help or support resource accessed by a student (FAQ, tutorial, writing center guide). broadMatch: `http://adlnet.gov/expapi/activities/media`.
8. `https://w3id.org/xapi/aclit/v1/activity-types/extension-request` -- "Extension Request" -- a request for an assessment deadline extension. broadMatch: none.
9. `https://w3id.org/xapi/aclit/v1/activity-types/self-report` -- "Self-Report Instrument" -- a self-report questionnaire (confidence, goal-setting, strategy-use). This is a placeholder; detailed schema deferred to Phase 3. broadMatch: `http://adlnet.gov/expapi/activities/question`.

**Concepts array -- Extensions (~15 custom):**

For each, include: `id`, `type` ("ContextExtension" or "ResultExtension"), `inScheme`, `prefLabel`, `definition`, `recommendedVerbs` (array of verb IRIs), and `inlineSchema` (stringified JSON Schema).

Context Extensions:
1. `device-type` -- ContextExtension -- "The type of device used for the learning session (smartphone, tablet, desktop, laptop)." -- inlineSchema: `{"type":"string","enum":["smartphone","tablet","desktop","laptop"]}`
2. `connection-type` -- ContextExtension -- "The network connection type during the session (wifi, mobile-data, offline, unknown)." -- inlineSchema: `{"type":"string","enum":["wifi","mobile-data","offline","unknown"]}`
3. `help-type` -- ContextExtension -- "The category of help-seeking interaction (tutor-question, support-center, peer-help, self-help-resource)." -- inlineSchema: `{"type":"string","enum":["tutor-question","support-center","peer-help","self-help-resource"]}`

Result Extensions (Writing):
4. `word-count` -- ResultExtension -- "Number of words in submitted text." -- inlineSchema: `{"type":"integer","minimum":0}`
5. `paragraph-count` -- ResultExtension -- "Number of paragraphs in submitted text." -- inlineSchema: `{"type":"integer","minimum":0}`
6. `citation-count` -- ResultExtension -- "Number of citations or references in submitted text." -- inlineSchema: `{"type":"integer","minimum":0}`
7. `draft-number` -- ResultExtension -- "The draft iteration number (1 = first draft, 2 = second draft, etc.)." -- inlineSchema: `{"type":"integer","minimum":1}`
8. `assignment-type` -- ResultExtension -- "The type of writing assignment (paragraph, essay, reflection, report, summary, annotated-bibliography)." -- inlineSchema: `{"type":"string","enum":["paragraph","essay","reflection","report","summary","annotated-bibliography"]}`
9. `time-spent` -- ResultExtension -- "ISO 8601 duration representing time spent on the activity." -- inlineSchema: `{"type":"string","pattern":"^P(?!$)(\\d+Y)?(\\d+M)?(\\d+D)?(T(?=\\d)(\\d+H)?(\\d+M)?(\\d+(\\.\\d+)?S)?)?$"}`
10. `artifact-url` -- ResultExtension -- "URL reference to an externally stored writing artifact (used for text > 1000 chars)." -- inlineSchema: `{"type":"string","format":"uri"}`
11. `artifact-content-type` -- ResultExtension -- "MIME type of the externally stored artifact." -- inlineSchema: `{"type":"string"}`
12. `prompt-topic` -- ResultExtension -- "The writing prompt or topic the student is responding to." -- inlineSchema: `{"type":"string"}`

Result Extensions (Discussion):
13. `thread-depth` -- ResultExtension -- "The nesting depth of a forum reply (0 = top-level post, 1 = direct reply, 2+ = nested reply)." -- inlineSchema: `{"type":"integer","minimum":0}`

Result Extensions (Peer Review):
14. `feedback-text` -- ResultExtension -- "The text of feedback provided in a peer review." -- inlineSchema: `{"type":"string"}`
15. `reviewer-pseudonym` -- ResultExtension -- "The pseudonymous identifier of the peer reviewer (matches actor account naming)." -- inlineSchema: `{"type":"string"}`

The `templates` and `patterns` arrays should be empty arrays `[]` in this plan -- they will be populated in Plan 02-02.

**Formatting:** Valid JSON-LD. Use 2-space indentation. Ensure all IRIs are complete (no abbreviations in the JSON). The file must parse as valid JSON.
  </action>
  <verify>
Verify the file exists and is valid JSON: run `node -e "JSON.parse(require('fs').readFileSync('xapi-profile/academic-literacies-profile.jsonld','utf8')); console.log('Valid JSON')"` from the project root. Grep for `conformsTo` -- must appear. Grep for `activity-types/session` -- must appear. Count activity type concepts (grep for `"type": "ActivityType"` or `"ActivityType"`) -- should be 9. Count extension concepts (grep for `Extension`) -- should be at least 15.
  </verify>
  <done>
A valid JSON-LD xAPI Profile document exists with profile metadata, 9 custom activity types with definitions and broadMatch references, and ~15 custom extensions with inlineSchema validation. Templates and patterns arrays are empty (populated in Plan 02-02). The file parses as valid JSON.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Vocabulary Mapping and Event Catalog with DPIA References</name>
  <files>
    xapi-profile/docs/vocabulary-mapping.md
    xapi-profile/docs/event-catalog.md
  </files>
  <action>
Create xapi-profile/docs/ directory.

**File 1: xapi-profile/docs/vocabulary-mapping.md**

Create a document that maps every verb and activity type used in the profile to its source vocabulary. This is the provenance record.

Structure:
1. **Introduction** -- Purpose: trace every vocabulary term to its authoritative source. Design principle: maximize reuse, minimize custom vocabulary.

2. **Verbs Used (All Reused -- Zero Custom)** -- Table with columns: Verb Display Name | Verb IRI | Source Vocabulary | Used For (event types)

Include ALL verbs from the event inventory:
- loggedin | `https://brindlewaye.com/xAPITerms/verbs/loggedin` | Jisc/Brindlewaye | Session open
- loggedout | `https://brindlewaye.com/xAPITerms/verbs/loggedout` | Jisc/Brindlewaye | Session close
- viewed | `http://id.tincanapi.com/verb/viewed` | TinCan | Page/resource viewed, reading section viewed, thread viewed, help resource accessed
- experienced | `http://adlnet.gov/expapi/verbs/experienced` | ADL | Module navigation
- completed | `http://adlnet.gov/expapi/verbs/completed` | ADL | Reading section completed, quiz completed, module completed
- attempted | `http://adlnet.gov/expapi/verbs/attempted` | ADL | Quiz attempted
- passed | `http://adlnet.gov/expapi/verbs/passed` | ADL | Quiz passed
- failed | `http://adlnet.gov/expapi/verbs/failed` | ADL | Quiz failed
- answered | `http://adlnet.gov/expapi/verbs/answered` | ADL | Question answered
- create | `http://activitystrea.ms/schema/1.0/create` | Activity Streams | Forum post created
- commented | `http://adlnet.gov/expapi/verbs/commented` | ADL | Forum reply posted
- submit | `http://activitystrea.ms/schema/1.0/submit` | Activity Streams | Writing submitted, peer review submitted, self-report submitted
- asked | `http://adlnet.gov/expapi/verbs/asked` | ADL | Help requested, extension requested
- progressed | `http://adlnet.gov/expapi/verbs/progressed` | ADL | Course progressed

3. **Activity Types -- Reused from Standard Vocabularies** -- Table with columns: Type Name | Type IRI | Source | Used For
- course | `http://adlnet.gov/expapi/activities/course` | ADL | Course context
- module | `http://adlnet.gov/expapi/activities/module` | ADL | Module context, module navigation, module completed
- assessment | `http://adlnet.gov/expapi/activities/assessment` | ADL | Quiz attempt
- cmi.interaction | `http://adlnet.gov/expapi/activities/cmi.interaction` | ADL/SCORM | Quiz question
- page | `http://activitystrea.ms/schema/1.0/page` | Activity Streams | Page/resource viewed
- note | `http://activitystrea.ms/schema/1.0/note` | Activity Streams | Forum post (new thread)
- comment | `http://activitystrea.ms/schema/1.0/comment` | Activity Streams | Forum reply

4. **Activity Types -- Custom (Academic Literacies Profile)** -- Table: Type Name | Type IRI | Rationale (why no standard type fits)
List all 9 custom activity types with a brief justification for why they are custom.

5. **Extensions -- Custom (Academic Literacies Profile)** -- Table: Extension Name | Extension IRI | Type (Context/Result) | Rationale
List all ~15 extensions.

6. **Design Decisions**
- Zero custom verbs: all events map to existing standard verbs via verb + activity type combination
- IRI namespace: `https://w3id.org/xapi/aclit/v1/` with semantic versioning
- xAPI 1.0.3 targeting with 2.0 forward-compatibility

**File 2: xapi-profile/docs/event-catalog.md**

Create the human-readable event catalog that serves as the primary reference for all trackable events. This document satisfies Success Criterion 4 by linking every event to the DPIA.

Structure:
1. **Introduction** -- This catalog lists every event type captured by the Academic Literacies learning analytics system. Each event documents its pedagogical purpose and references the DPIA section that authorizes its collection.

2. **Event Categories** -- Organize events by category. For EACH event, document:
   - **Event name** (human-readable)
   - **Verb** (display name + IRI)
   - **Activity Type** (display name + IRI)
   - **Description** (what triggers this event)
   - **DPIA Reference** (section number from governance/dpia/01-processing-description.md, e.g., "DPIA 2.1: LMS Interaction Events")
   - **Pedagogical Purpose** (1-2 sentences: how collecting this helps the student -- this is MANDATORY per Phase 1 DPIA and Success Criterion 4)
   - **Metric Domain** (which downstream metric area uses this: Engagement, CoP, SDL, or multiple)
   - **Key Extensions** (custom extensions used by this event type)

Categories and events to document (use the Complete Event Type Inventory from 02-RESEARCH.md):

**Navigation Events** (4 events): session open, session close, page/resource viewed, module navigation
- DPIA ref: map to "Session Metadata" and "LMS Interaction Events" sections
- Pedagogical purpose: Session patterns reveal engagement regularity; early identification of disengaging students

**Reading Events** (2 events): section viewed, section completed
- DPIA ref: map to "LMS Interaction Events"
- Pedagogical purpose: Section-level tracking identifies where students struggle; reading completion correlates with assessment readiness

**Quiz Events** (5 events): quiz attempted, quiz completed, quiz passed, quiz failed, question answered
- DPIA ref: map to "Quiz/Assessment Records"
- Pedagogical purpose: Performance trends inform tutoring focus areas; question-level data reveals specific misconceptions

**Writing Events** (1 event): writing submitted
- DPIA ref: map to "Writing Sample Submissions"
- Pedagogical purpose: Draft progression reveals writing development trajectory; writing artifacts enable academic discourse analysis (CoP metric)

**Discussion Events** (3 events): forum post created, forum reply posted, thread/post viewed
- DPIA ref: map to "Discussion Forum Activity"
- Pedagogical purpose: Participation patterns track movement from peripheral to full CoP participation; reply depth indicates engagement quality

**Peer Review Events** (1 event): peer review submitted
- DPIA ref: map to "Discussion Forum Activity" (peer interaction category)
- Pedagogical purpose: Peer review is a key CoP indicator -- giving and receiving feedback develops academic community membership

**Help-Seeking Events** (3 events): help requested, help resource accessed, extension requested
- DPIA ref: map to "LMS Interaction Events" (help-seeking subcategory)
- Pedagogical purpose: Help-seeking patterns distinguish strategic from dependent/avoidant learners (SDL metric); extension requests may signal time management challenges

**Curriculum Progress Events** (2 events): module completed, course progressed
- DPIA ref: map to "Curriculum Position/Progress"
- Pedagogical purpose: Progress pacing relative to expected trajectory identifies students falling behind before assessment reveals it

**Self-Report Events** (1 event -- placeholder): self-report submitted
- DPIA ref: map to "Self-Report Questionnaire Responses"
- Pedagogical purpose: Self-reported confidence, goals, and strategies enable calibration accuracy measurement (SDL metric); placeholder pending Phase 3 instrument design

3. **Summary Statistics**
- Total event types: 22
- Standard verbs reused: 14
- Custom verbs: 0
- Custom activity types: 9
- Custom extensions: ~15

Note: The DPIA section references must match the actual section headings in governance/dpia/01-processing-description.md. Read that file to get the exact section numbers and headings before writing the references. If the DPIA uses different category names, adapt to match.
  </action>
  <verify>
Both files exist. Grep for "ADL" in vocabulary-mapping.md -- must appear. Grep for "Pedagogical Purpose" in event-catalog.md -- must appear at least 9 times (once per event category). Grep for "DPIA" in event-catalog.md -- must appear at least 9 times. Verify event-catalog.md covers all 22 event types by grepping for key events: "session open", "quiz completed", "writing submitted", "forum post", "peer review", "help requested", "module completed", "self-report".
  </verify>
  <done>
Vocabulary mapping traces all 14 verbs and 7 standard activity types to their source vocabularies, lists all 9 custom activity types with justification, and documents all ~15 custom extensions. Event catalog documents all 22 event types with DPIA references, pedagogical purpose statements, metric domain mapping, and key extensions. Every event type links to the Phase 1 DPIA.
  </done>
</task>

</tasks>

<verification>
1. xapi-profile/academic-literacies-profile.jsonld exists and is valid JSON
2. Profile contains 9 custom activity types and ~15 custom extensions
3. All extensions have inlineSchema with JSON Schema validation
4. Zero custom verbs are defined
5. Vocabulary mapping covers all verbs and activity types with source attribution
6. Event catalog documents all 22 event types with DPIA references
7. Every event has a pedagogical purpose statement
8. IRI namespace uses semantic versioning (v1 in path)
</verification>

<success_criteria>
A machine-readable xAPI Profile document defines all custom vocabulary. A vocabulary mapping provides full provenance for every term. An event catalog links every event to the DPIA and documents its pedagogical purpose. A developer could read these three documents and understand what vocabulary exists, where it comes from, and why each event is collected.
</success_criteria>

<output>
After completion, create `.planning/phases/02-xapi-event-schema/02-01-SUMMARY.md`
</output>
