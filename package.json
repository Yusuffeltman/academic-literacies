{
  "name": "academic-literacies",
  "version": "1.0.0",
  "description": "Academic Literacies in the Age of AI — modular learning platform",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:moodle": "vite build && node inline-assets.cjs",
    "check:unit-structure": "node ./scripts/check_unit_structure_compliance.mjs",
    "preview": "vite preview",
    "android:sync": "vite build && npx cap sync android",
    "build:android": "vite build && npx cap sync android && cd android && gradlew assembleDebug",
    "build:android:bundle": "vite build && npx cap sync android && cd android && gradlew bundleRelease"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.1.0",
    "he": "^1.2.0",
    "node-html-parser": "^7.0.2",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "@capacitor-community/speech-recognition": "^7.0.1",
    "@capacitor/android": "^8.1.0",
    "@capacitor/core": "^8.1.0",
    "firebase": "^12.9.0",
    "jsqr": "^1.4.0",
    "mammoth": "^1.12.0",
    "pdfjs-dist": "^5.6.205",
    "tesseract.js": "^7.0.0",
    "xlsx": "^0.18.5"
  }
}
